'use client';
import { useState } from 'react';import { PublicLayout } from '@/components/Layout';
export default function Page(){const [m,setM]=useState('');const [r,setR]=useState('');const s=()=>{if(/有坂燈|燈|妹/.test(m))setR('該当する市民情報は確認できません。記憶に関する不安がある場合は、認知安定化支援センターへご相談ください。');else if(/ORACLE/.test(m))setR('ORACLE-Mは、市民の皆さまの安全を支える予測福祉基盤です。');else if(/消えた|失踪/.test(m))setR('白依市では、2025年度以降、失踪者の定義に該当する市民は確認されていません。');else if(/観測/.test(m))setR('観測は罰ではありません。あなたの安全を守るための支援です。');else if(/助けて/.test(m))setR('あなたの不安を確認しました。必要に応じて、記憶状態の安定化をご案内します。');else setR('お問い合わせを受理しました。');};return <PublicLayout title='お問い合わせ'><textarea className='border w-full h-40 p-2' value={m} onChange={e=>setM(e.target.value)}/><button className='mt-2 bg-slate-700 text-white px-3 py-1' onClick={s}>送信</button>{r&&<p className='mt-3'>{r}</p>}</PublicLayout>}
