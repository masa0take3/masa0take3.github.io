'use client';import { useState } from 'react';import { useRouter } from 'next/navigation';import { SecretLayout } from '@/components/Layout';import { loadProgress, saveProgress } from '@/lib/progress';
export default function P(){const [v,setV]=useState('');const [m,setM]=useState('');const r=useRouter();const ok=()=>{if(v.replace(/\s/g,'')==='有坂燈'){const p=loadProgress();p.enteredHikariName=true;saveProgress(p);setM('外部観測者による記憶保持を確認しました。');setTimeout(()=>r.push('/final/choice'),600);}else setM('記録と一致しません。もう一度、思い出してください。');};return <SecretLayout title='名前入力'><p>彼女の名前を入力してください。</p><input className='border p-2' value={v} onChange={e=>setV(e.target.value)}/><button className='ml-2 border px-2' onClick={ok}>確認</button><p>{m}</p></SecretLayout>}
